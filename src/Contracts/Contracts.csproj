<Project Sdk="Microsoft.NET.Sdk">

  <!--
    プロセスをまたぐ契約。依存ゼロ。

    ブローカーと、外から繋ぐ側の両方がこれを参照する。Domain も Runtime も参照しない ──
    参照すると、外から繋ぐだけの側に SQLite や配送エンジンが付いてくる。
    値の検証（JSON か・64KB 以下か）はブローカーが Domain で行うので、
    ここには型の形しか置かない。
  -->

</Project>
