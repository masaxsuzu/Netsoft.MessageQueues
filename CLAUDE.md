# Netsoft.MessageQueues

単一コンピュータ上で動作するメッセージキュー基盤。プロセス内の購読者へ、
永続化されたメッセージを at-least-once で配送する。動かし方は [README](./README.md) を参照。

構成と開発の作法は [Netsoft.Jobs](https://github.com/masaxsuzu/Netsoft.Jobs) を手本にしている。
向こうで数えて決まった規則は、こちらでも同じ理由で採る。

## 開発サイクル

1. **依頼** — 利用者が自然言語で伝える。粒度は問わない
2. **タスク化** — Claude が実装単位に分解し、**何を作るかについて着手前に合意を取る**。
   **どう作るかは合意の対象ではない**（下の「何を聞き、何を聞かないか」）
3. **実装** — 1 タスクずつ。受け入れ条件・触ってよいファイルの範囲・
   **build と test を通すまでがタスク**であることを明確にする。
   **実装は必ず 1 本ずつ。並行させない** ── 作業ツリーは 1 つしか無い。
   並行してよいのはツリーを書き換えない読み取り専用の調査・レビューだけ
4. **検査** — **手元のゲート（`tools/gate.sh`）が検査する。** build / test / format を
   コミットした木で通す。通っていないコミットは push できない
   （`tools/gate-hook.sh` と `tools/git-hooks/pre-push` が止める）。
   **落ちたまま次のタスクへ進まない**。
   CI（GitHub Actions）は**同じ 3 コマンドを同じ順で**回すだけで、CI にしか無い検査は作らない
   （手本の Netsoft.Jobs は CI を持たない選択をしているが、こちらは持つ ── 利用者の決定。
   ただし「手元で通してから push する」規律は CI が在っても変わらない）
5. **PR** — 1 タスク = 1 PR
6. **マージ** — **PR を作ったら常に auto-merge（squash）を有効化する**。CI が緑になれば
   GitHub が自動でマージする。`needs-review` を付けた PR だけ、有効化せずに置く。
   auto-merge が成立するのは CI が在るからで（checks が pending の間しか有効化できない）、
   CI を持たない手本の Netsoft.Jobs では使えず、あちらは Claude が手で押している

### 何を聞き、何を聞かないか

聞いてよいのは**利用者から見えること**だけ。何が配送され、失敗したらどうなるか、
何を捨ててよいか。**実装の選び方は聞かない** ── 決めて、置いた仮定を PR に書く。
読んで違っていれば直せばよい。

判定はこう。**答えが API の見え方や配送の意味論を変えるなら聞く。コードの形しか変えないなら聞かない。**

### マージと needs-review

受け入れ条件を満たし、ゲートが通っているなら、**既定は auto-merge を有効化する**
（マージそのものは CI の緑を待って GitHub が行う）。
`needs-review` を付けたものだけ、有効化せずに置く。

`needs-review` を付けるのは、**その変更が今後の判断を縛る規則を新しく決めているとき**だけ。
次のどれかに当たるかで見る。

- **既存の決定を覆す** — 前に「こうする」と決めた理由を、別の理由で置き換えている
- **利用者にしか決められない選び方を含む** — 何を配送し、何を捨て、どこで妥協するか。
  実装の都合では決まらず、依頼にも書かれていなかったもの
- **外から見える約束を変える** — API の形、配送の意味論、既存データの意味

当てはまらないならマージする。**大きい・難しい・時間がかかったは理由にならない。**

**3 つのどれかに当たっても、利用者がその場で名指しで決めたことなら付けない。**

**迷ったら、迷った理由を PR に書いてからマージする。**

## やらないこと

- `main` への直接 push。変更は必ず PR を通す
- `needs-review` を付けた PR の auto-merge を有効化すること。あれは「利用者が決めるべき
  ことが入っている」という申告なので、申告した本人が流しては申告した意味が無い

## どこに何を書くか

同じことを 4 か所に書かない。読み手が探す場所が決まっている。

- **コード** — How。どうやっているか。コードだけが実際に動くので、ここに嘘は書けない
- **テストコード** — What。何が守られているか。テスト名が仕様の見出しになる
- **コミットログ** — Why。なぜ変えたか。差分からは読めない、変更を必要とした事情を残す
- **コードコメント** — Why not。**なぜそうしなかったか**。選ばなかった書き方、
  順序を入れ替えてはいけない理由、過去に試して壊れたこと。
  How をなぞるコメント（コードを読めば分かること）は書かない

## この基盤が約束していること

実装を触る前に押さえること。詳細は [docs/operating.md](./docs/operating.md)。

- **at-least-once** — 発行が返った時点でメッセージは永続化されており、購読者の処理が
  正常に終わるまで配送済みにならない。**同じメッセージが 2 度届くのは正常**で、
  購読者は冪等に書く
- **購読はプロセス内でもプロセス外でもよい** — 処理を書く側は同じ `IMessageSubscriber`。
  外から繋ぐ場合はブローカー（`src/Broker`）を 1 つ立て、HTTP で発行し SSE で受け取る。
  **どちらの場合も購読の一覧は起動時に静的に決まり、接続では増えない**
- **ペイロードは JSON、UTF-8 で 64KB まで** — 検証は `MessagePayload` を作る瞬間に済み、
  下流に判定は現れない
- **順序はキーごとに発行順** — 同じパーティションキーは同じレーンへ落ち、レーンの中は直列。
  既定はレーン 1 本で購読全体が発行順。並列が欲しければ購読者が `Lanes` を増やす

## ディレクトリ構造

- `src/Domain` — メッセージ・配送のエンティティと値、保存の口（`IMessageStore`）。
  **依存ゼロ**が線で、置けるかどうかはこれで決まる
- `src/Runtime` — 実行基盤。発行（`MessagePublisher`）・配送（`DeliveryEngine`）・
  購読者の契約（`IMessageSubscriber`。利用者が書く唯一の拡張点）。
  **ASP.NET を参照しない**ので HTTP の型に手が届かない
- `src/Infrastructure` — SQLite。実装するのは Domain の口だけ
- `src/Contracts` — プロセスをまたぐ契約（経路と線の上の型）。**依存ゼロ**
- `src/Broker` — 発行と配送の口を HTTP で開くホスト。**ASP.NET を参照してよいのはここだけ**
- `src/Client` — ブローカーへ外から繋ぐ側。利用者が書くのはプロセス内と同じ `IMessageSubscriber`
- `tests/` — src と 1:1 で対応。テストプロジェクト名は src と同名で、
  `.Tests` の接尾辞は `tests/Directory.Build.props` が付ける。
  例外は `tests/E2E` で、**ブローカーを本物の別プロセスとして起こす**（境界を確かめる唯一の層）
- `docs/` — 開発の決めごと。**作業を始める前に読むこと**

## docs

- [build.md](./docs/build.md) — ビルド・テストの規則と、テストの後始末
- [conventions.md](./docs/conventions.md) — コーディング規約と命名
- [operating.md](./docs/operating.md) — 配送の意味論と、購読者を書く人への契約
